<%
// FIXME: permissions, ban check?

Forum.root.data.topic();

topic = db.forum.topics.findOne( { _id : request.topic } );

if(topic.hidden) {
    topic.hidden = false;
}
else {
    topic.hidden = true;
}

db.forum.topics.save(topic);

%>
